<div class="container">

  <h3>Add a new bookmark</h3>
    <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.submit "Submit bookmark", class: "btn" %>
    <% end %>
</div>
